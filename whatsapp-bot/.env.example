# ─── WhatsApp Bot ─────────────────────────────────────────────
# Salin jadi .env:  cp .env.example .env

# Port server bot (default 3000)
PORT=3000

# Nama folder sesi Baileys (kredensial login WA tersimpan di sini —
# folder ini di-gitignore, jangan pernah di-commit!)
WHATSAPP_SESSION=whatsapp-bot
